python -m venv .venv
source .venv/bin/activate      # on Windows: .venv\Scripts\activate
pip install -r requirements.txt   # later
# to create requirements.txt after installing dependencies:
pip freeze > requirements.txt
